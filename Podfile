# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ComediansContest' do
  use_frameworks!

  # Rx
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxDataSources', '~> 3.0'

  pod 'IGListKit', '~> 3.1'

  # Firebase
  #pod 'Firebase/Core'
  #pod 'Firebase/Auth'
  #pod 'Firestore', :podspec => 'https://storage.googleapis.com/firebase-preview-drop/ios/firestore/0.7.0/Firestore.podspec.json'
  #pod 'Firebase/DynamicLinks'
  #pod 'Firebase/AdMob'

  target 'ComediansContestTests' do
    inherit! :search_paths
  end

  target 'ComediansContestUITests' do
    inherit! :search_paths
  end

end
